IP address: username@144.17.92.10

docker build context:

- every file that the `docker build` command knows about

Dockerfile is pre image compilation
docker-compose is post image compilation
